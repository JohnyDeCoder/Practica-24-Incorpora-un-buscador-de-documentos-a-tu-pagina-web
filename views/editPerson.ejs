<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" /> <!-- this is the character encoding -->
    <title>Formulario para editar y actualizar</title>
</head>

<body>
    <h1>Actualizar Persona</h1>
    <form action="/updatePerson" method="POST"> <!-- action is the route that will handle the form data -->
        <p>NSS: <input type="number" id="nss" name="nss" value="<%= `${person.nss}` %>" /></p>
        <p>Nombre: <input type="text" id="nombre" name="nombre" value="<%= `${person.nombre}` %>" /></p>
        <p>Edad: <input type="number" id="edad" name="edad" value="<%= `${person.edad}` %>" /></p>
        <p>Tipo de sangre: <input type="text" id="tipoSangre" name="tipoSangre" value="<%= `${person.tipoSangre}` %>" />
        </p>
        <p>ID: <input type="text" id="objId" name="objId" value="<%= `${person._id}` %>" /></p>
        <p><input type="submit" value="Submit Query and Update" /></p>
        <!-- type="submit" is the button that will submit the form -->
    </form>
</body>

</html>